# Binary Search Strategy

When the range is still wide, a binary-search style guess is usually the fastest path to the secret number.

Hint: Use a binary-search split to cut the remaining search space in half.
